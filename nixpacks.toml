[phases.setup]
aptPkgs = ["ffmpeg", "python3-pip", "build-essential"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[phases.build]
cmds = ["python -c \"import app; print('Build successful')\""]

[start]
cmd = "gunicorn -k eventlet -w 1 --bind 0.0.0.0:$PORT app:app"
